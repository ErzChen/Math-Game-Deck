// No built-in problems yet — add some from the "Manage Questions" panel.
// { tier, q, a, e } — tier (1-4) is just a display badge here, unlike
// Ladder it doesn't affect scoring (elimination rounds are flat +1).
const builtinElimination = [];
