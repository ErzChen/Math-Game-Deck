// No built-in problems yet — add some from the "Manage Questions" panel.
// { q, a, e } — "a" is the exact numeric answer, judged by the host
// against teams' whiteboard guesses (closest / exact).
const builtinEstimation = [];
